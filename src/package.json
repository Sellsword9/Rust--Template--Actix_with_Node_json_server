{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "json-server --watch db/src.json"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^5.0.0"
  }
}